requests>=2.28.0,<3.0.0
colorama>=0.4.6,<0.5.0

# Optional — installed per platform:
# pyttsx3>=2.90       (Linux/Windows TTS — pip install pyttsx3)
# pywin32>=306        (Windows TTS/COM — pip install pywin32)
# winrt-Windows.Devices.Geolocation  (Windows GPS)
# gpsd-py3>=0.3.0     (Linux GPS via gpsd — pip install gpsd-py3)
# piper-tts>=1.2.0    (Neural TTS — pip install piper-tts)

# Dev only:
# pyinstaller>=6.0    (pip install pyinstaller)
